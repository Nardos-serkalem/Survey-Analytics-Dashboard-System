## clone the repo and navigate to the folder
```bash
cd Survey-Analytics-Dashboard-System
```
then create and setup the virtual envirinment
```bash
python -m venv venv
```
install the requirement
```bash
pip install -r requirements.txt
```
run the app
```bash
python run.py
```
